name: Auth Dev CD

on:  
    push: 
      branches: 
         - develop
         - main
      paths: 
         - 'proto/**'
         - '**.proto'
         - '**/buf.yaml'
         - '**/buf.lock'
         - '**/buf.md'               
jobs:
    buf-auth-cd:
        uses: LLM-Finetuning-platform/shared-workflows/.github/workflows/buf-cd.yaml@main
