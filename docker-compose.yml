services:
    postgres:
      ports:
        - '5432:5432' 
      env_file:
        - .env.dev

      image:  postgres:14-alpine
      container_name: PostgresContainer
      restart: always
      volumes:
        - postgres_db:/var/lib/postgresql/data

volumes:
  postgres_db:
      driver: local

  